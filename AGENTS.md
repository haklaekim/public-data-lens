# 에이전트 공통 규칙 (구현·리뷰 공용)

이 저장소에서 작업하는 모든 코딩 에이전트(Claude Code=구현, Codex 등=리뷰)에게 적용된다.
역할 고정: **구현자만 파일을 수정하고, 리뷰어는 읽기 전용으로 보고만 한다.**
상세 설계는 중복 기술하지 않는다 — 아래 기준 문서가 단일 출처다.

## 기준 문서 (규칙의 단일 출처)

- 아키텍처·동결 원칙: `docs/공공데이터_내비게이터_설계서_v1.0_확정판.md` (동결 — 수정 금지)
- 공개 계약: `docs/부속명세_v1.0.md` + `apps/server/datanav/spec/` (semver — required 제거·의미 변경은 breaking, 재승인 필요)
- 관측 계층(S1~S3): `docs/데이터구조_관측_설계_v2_초안.md` — **v2.2 판정표(§0.1·§0.2)가 리뷰 체크리스트**

## 불변 규칙 (리뷰 시 이 위반만 P0으로)

- **원본 보존**: 원본 컬럼명·예시값을 정규화·trim·번역하지 않는다. 내부 파싱용 처리와 외부 반환값을 구분한다.
- **관측 모델 경계**: CatalogRecord ≠ SourceAsset ≠ StructureObservation ≠ DataTable. ZIP 멤버·XLSX 시트는 각각 독립 DataTable. 관측은 불변 — 재관측은 새 observation 생성.
- **안전 선행**: 안전·라이선스 검사를 통과하지 않은 예시값을 영구 저장하거나 응답에 포함하지 않는다. 다운로드 URL의 토큰·키·서명 파라미터를 외부 응답에 넣지 않는다. API 인증키는 저장 자체 금지.
- **계약 호환**: 기존 MCP Tool·응답 필드를 제거하거나 의미를 바꾸지 않는다. 스키마 변경 시 `python scripts/gen_tool_spec.py` 재생성(라이브 검증 포함) 필수.
- **상태 ≠ 오류**: 미수집(NOT_COLLECTED)·라이선스 보류·안전 차단은 정상 응답이다. 서버 오류로 처리하지 않는다.
- **판정에는 규칙 버전**: 모든 판정·관찰 필드는 rules 레지스트리의 ruleId를 동반한다.

## 검증 기준선

```bash
cd apps/server && ../../.venv/bin/python -m pytest    # 카탈로그 있음: 111 통과
# 신선한 클론(카탈로그 미빌드): 91 통과 / 20 건너뜀 / 0 실패
```

기준선을 깨는 변경은 병합 불가. 생성 데이터(`data/`)·릴리스 산출물은 리뷰 대상에서 제외.

## 리뷰 요청 표준문

> 코드를 수정하지 말고 main 대비 변경분을 검토하라. 기준: ①관측 설계 v2.2 판정표(§0.1·§0.2)
> 일치 ②위 불변 규칙 ③기존 계약 하위 호환 ④오류 처리·테스트 누락. 실질 문제만 P0/P1/P2로,
> 파일·위치·발생 조건·영향·수정 방향 포함. 스타일·취향은 보고하지 마라.
> 문제가 없으면 "no material findings"라고 명시하라.
